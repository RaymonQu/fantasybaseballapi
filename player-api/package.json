{
  "name": "@draftkit/player-api",
  "version": "0.1.0",
  "private": true,
  "description": "Player API backend for DraftKit barebones scaffold",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "seed": "node src/scripts/seed.js",
    "test": "node --test",
    "lint": "node -e \"console.log('No lint rules configured for @draftkit/player-api')\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "mongoose": "^8.9.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
